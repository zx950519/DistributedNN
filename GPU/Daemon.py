# coding=utf-8
import os, time

if __name__ == "__main__":

    # if os.path.exists("Daemon.ini"):
    #     file = os.open("Daemon.ini")

    while True:
        if os.path.exists(""):

            continue

        time.sleep(5)